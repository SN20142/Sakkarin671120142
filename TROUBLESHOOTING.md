# 🔧 คู่มือแก้ไขปัญหา HR System

## 📝 ก่อนจะแก้ไข - ให้ทำตามนี้ก่อน

### ✅ Checklist ตรวจสอบพื้นฐาน:

```
[ ] Apache/Nginx web server กำลังทำงาน
[ ] MySQL database server กำลังทำงาน
[ ] PHP version 7+ ติดตั้งแล้ว
[ ] ไฟล์ทั้งหมดอยู่ใน web root (เช่น htdocs สำหรับ XAMPP)
[ ] เปิด Browser Console (F12) เพื่อดู error messages
```

---

## 🚨 ปัญหาทั่วไปและวิธีแก้ไข

### **ปัญหา #1: "ไม่สามารถติดต่อเซิร์ฟเวอร์ได้"**

**สาเหตุที่เป็นไปได้:**
1. Web Server (Apache) ไม่ทำงาน
2. PHP ไม่ติดตั้ง หรือปิดอยู่
3. ไฟล์ .php อยู่ผิดตำแหน่ง
4. CORS issue (หากใช้ domain ต่าง)

**วิธีแก้ไข:**

**สำหรับ XAMPP on Windows:**
```
1. เปิด XAMPP Control Panel
2. ตรวจสอบ Apache: Status ต้องแสดง "Running" (สีเขียว)
3. ตรวจสอบ MySQL: Status ต้องแสดง "Running" (สีเขียว)
4. ถ้ายังไม่ทำงาน:
   - คลิก "Stop" แล้วรอสักครู่
   - คลิก "Start" ใหม่
5. เปิด Browser ไปที่ http://localhost/
   - ควรเห็น XAMPP dashboard
```

**สำหรับ WAMP on Windows:**
```
1. คลิกไอคอน WAMP ที่ system tray
2. เลือก "Start All Services"
3. ตรวจสอบสีไอคอนควรเป็นสีเขียว (ไม่สีแดง/ส้ม)
4. Hover icon ควรแสดง "All services running"
```

**ตรวจสอบตำแหน่งไฟล์:**
- XAMPP: `C:\xampp\htdocs\your-project\`
- WAMP: `C:\wamp64\www\your-project\`

---

### **ปัญหา #2: "Database connection failed"**

**สาเหตุที่เป็นไปได้:**
1. MySQL Server ไม่ทำงาน
2. Credentials ไม่ถูกต้อง (username/password)
3. Database 'hr_system' ไม่มีอยู่
4. Port MySQL ไม่ใช่ 3306 (หรือ credentials ต่างกัน)

**วิธีแก้ไข:**

**Step 1: ตรวจสอบ MySQL เทียบ:**
```
สำหรับ XAMPP:
1. เปิด XAMPP Control Panel
2. คลิก "Shell" 
3. พิมพ์:
   mysql -u root
   (ไม่มีรหัสผ่าน)
4. ถ้า prompt แสดง "mysql>" แปลว่าเชื่อมต่อสำเร็จ
5. ออกด้วยพิมพ์: exit
```

**Step 2: ตรวจสอบ Credentials ใน db_connect.php:**
```php
$servername = "localhost";  // ค่าเริ่มต้น
$username = "root";         // ค่าเริ่มต้น XAMPP/WAMP
$password = "";             // ส่วนใหญ่ว่างเปล่า
$dbname = "hr_system";      // ชื่อที่เราสร้าง
```

**Step 3: สร้างฐานข้อมูลด้วยมือ (ถ้า auto-create ไม่ได้):**

ทำได้ 2 วิธี:

**วิธี A: ใช้ phpMyAdmin:**
```
1. เปิด Browser ไปที่ http://localhost/phpmyadmin
2. ล็อกอิน (ปกติ username=root, password ว่าง)
3. คลิก "New" เพื่อสร้างฐานข้อมูล
4. ชื่อ: hr_system
5. Collation: utf8mb4_unicode_ci
6. คลิก "Create"
7. เลือก database hr_system
8. ไปที่ tab "SQL"
9. คัดลอกเนื้อหาจาก create_table.sql และรัน
```

**วิธี B: ใช้ Command Line:**
```
1. เปิด XAMPP Shell (หรือ CMD)
2. พิมพ์:
   
   mysql -u root
   
3. ใน MySQL prompt พิมพ์:

   CREATE DATABASE IF NOT EXISTS hr_system 
   CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   USE hr_system;
   
   CREATE TABLE IF NOT EXISTS `employees` (
     `id` INT AUTO_INCREMENT PRIMARY KEY,
     `emp_id` VARCHAR(50) UNIQUE,
     `firstname` VARCHAR(100) NOT NULL,
     `lastname` VARCHAR(100) NOT NULL,
     `email` VARCHAR(100) UNIQUE NOT NULL,
     `password` VARCHAR(255) NOT NULL,
     `phone` VARCHAR(20),
     `birthdate` DATE,
     `department` VARCHAR(50),
     `position` VARCHAR(100),
     `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
     `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
     INDEX `idx_email` (`email`),
     INDEX `idx_emp_id` (`emp_id`),
     INDEX `idx_department` (`department`)
   ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
   
4. ออกด้วยพิมพ์: exit
```

---

### **ปัญหา #3: "Email already exists" เมื่อลองลงทะเบียน**

**สาเหตุ:** Email นี้ใช้ไปแล้ว

**วิธีแก้ไข:**

**ตัวเลือกที่ 1: ใช้ Email ใหม่**
- ลองลงทะเบียนด้วย email ที่ยังไม่เคยใช้

**ตัวเลือกที่ 2: ลบข้อมูลเก่า**

ใช้ phpMyAdmin:
```
1. เปิด http://localhost/phpmyadmin
2. คลิก database 'hr_system'
3. คลิก table 'employees'
4. เลือก row ที่ต้องการ (check box)
5. ด้านล่าง คลิก "Delete"
6. ยืนยัน "Yes"

หรือใช้ SQL:
1. ไปที่ tab SQL
2. พิมพ์ (เลือก email ที่ต้องการ):
   DELETE FROM employees WHERE email = 'old-email@example.com';
   
3. คลิก "Go"
```

---

### **ปัญหา #4: Login ไม่ได้ (Password ไม่ถูกต้อง)**

**สาเหตุที่เป็นไปได้:**
1. Password ไม่ตรง
2. Database connection มีปัญหา
3. Data corruption

**วิธีแก้ไข:**

**Step 1: ตรวจสอบ Password ที่ใช้ลงทะเบียน**
- Password ต้องมี 8 ตัวอักษรขึ้นไป
- ตรวจสอบการพิมพ์อักษรตัวเล็ก/ใหญ่

**Step 2: ลองลบและลงทะเบียนใหม่**
- ลบข้อมูล employee โดยใช้ phpMyAdmin
- ลงทะเบียนซ้ำใหม่

**Step 3: ตรวจสอบ password hash ใน database**
```
1. ใน phpMyAdmin
2. เปิด table 'employees'
3. ดู column 'password' 
   - ควรเห็น hash ยาวประมาณ 60 ตัวอักษร
   - เช่น: $2y$10$... (bcrypt format)
   - ถ้าเห็นข้อความธรรมดา = ไม่ได้เข้ารหัส (ปัญหา!)
```

---

### **ปัญหา #5: Dashboard ว่างเปล่า (ไม่เห็นรายชื่อพนักงาน)**

**สาเหตุที่เป็นไปได้:**
1. ยังไม่มีพนักงาน (ตาราว่าง)
2. Session หมดอายุ
3. Database ไม่เชื่อมต่อ
4. ไม่ได้ login

**วิธีแก้ไข:**

**Step 1: ตรวจสอบว่า login แล้ว**
- เข้าสู่ระบบใหม่

**Step 2: ลองลงทะเบียนพนักงาน 2-3 คน**
- ไปที่ register.html
- ลงทะเบียน 2-3 คน
- กลับ Dashboard (อาจต้อง refresh)

**Step 3: ดูใน Browser Console**
```
1. กด F12 เพื่อเปิด Developer Tools
2. ไปที่ tab "Console"
3. ตรวจสอบ error messages สีแดง
4. Copy error message มาถาม support
```

**Step 4: ตรวจสอบ database โดยตรง**
```
ใน phpMyAdmin:
1. เปิด database 'hr_system'
2. คลิก table 'employees'
3. ดู "Browse" tab
4. ควรเห็นรายการพนักงานที่ลงทะเบียน
5. ถ้าว่าง = ปัญหาใน save_employee.php
```

---

### **ปัญหา #6: ไม่สามารถแก้ไขข้อมูลได้**

**สาเหตุที่เป็นไปได้:**
1. Email ที่ใส่ซ้ำกับที่มีอยู่แล้ว
2. ข้อมูลไม่สมบูรณ์
3. Database error

**วิธีแก้ไข:**

**Step 1: ตรวจสอบความถูกต้องของข้อมูล**
- ชื่อต้องมี 2 ตัวอักษรขึ้นไป
- Email ต้องมีรูปแบบ valid (xxx@yyy.zzz)

**Step 2: ตรวจสอบ Email ซ้ำ**
- ใช้ email ที่ไม่เคยใช้มาก่อน
- หรือใช้ email เดิม (อย่าเปลี่ยน)

**Step 3: ดูใน Browser Console**
- กด F12 → Console
- ตรวจสอบ error message

---

### **ปัญหา #7: ไม่สามารถลบข้อมูลได้**

**สาเหตุที่เป็นไปได้:**
1. ยืนยันไม่ครบ (ต้อง click OK)
2. Database error
3. Permission issue

**วิธีแก้ไข:**

**Step 1: ตรวจสอบการยืนยัน**
- เมื่อคลิก "ลบ" ต้องมี dialog ถาม
- ต้อง click "OK" เพื่อตรวจสอบ

**Step 2: ดูใน Browser Console**
- กด F12 → Console
- ตรวจสอบ error message

**Step 3: ตรวจสอบว่าข้อมูลยังมีอยู่**
- ในตาราเพื่อดูว่าลบจริงหรือไม่

---

## 🧪 วิธีทดสอบด้วยตัวเอง

### **ทดสอบ #1: Database Connection**

ไปที่: `http://localhost/your-project/test_connection.php`

**ผลลัพธ์ที่ปกติ:**
```json
{
  "connection": "SUCCESS",
  "table_exists": "YES - employees table found",
  "table_columns": [
    "id (int)",
    "emp_id (varchar)",
    ...
  ],
  "prepare_success": "YES",
  "bind_success": "YES"
}
```

**ถ้ามี error:**
- ตรวจสอบ MySQL server
- ตรวจสอบ Credentials ใน db_connect.php

---

### **ทดสอบ #2: Browser Console**

1. เปิด register.html
2. กด F12 → Console tab
3. ลองกรอกฟอร์ม (ไม่ต้อง submit)
4. ดูว่ามี error สีแดง หรือปลายไม่

---

### **ทดสอบ #3: Network Request**

1. เปิด register.html
2. กด F12 → Network tab
3. กรอกฟอร์มและ submit
4. ตรวจสอบ request:
   - "save_employee.php" ควรมี status "200"
   - ใน Response tab ควรเห็น JSON

---

## 📊 ข้อมูล Debug

เมื่อติดปัญหา ให้ส่งข้อมูลเหล่านี้:

```
1. Web Server: XAMPP / WAMP / อื่นๆ
2. PHP Version: (run test_connection.php ดูได้)
3. MySQL Version: (ดูได้ใน phpMyAdmin)
4. Error Message: (copy แน่นอนจาก page หรือ console)
5. Browser: Chrome / Firefox / Edge / อื่นๆ
6. OS: Windows / Mac / Linux
7. Steps to Reproduce: (ลำดับขั้นตอนทำให้เกิด error)
```

---

## 🔐 Security Notes

⚠️ **สำหรับการใช้งานจริง (Production):**

1. **เปลี่ยน default MySQL password**
   ```sql
   mysql -u root
   ALTER USER 'root'@'localhost' IDENTIFIED BY 'new-strong-password';
   FLUSH PRIVILEGES;
   ```

2. **Update db_connect.php**
   ```php
   $password = "new-strong-password";
   ```

3. **ใช้ HTTPS** - ต้องมี SSL Certificate

4. **Add more validation** - ตรวจสอบ input ให้ครบถ้วน

5. **Rate limiting** - ป้องกัน brute force login

6. **CSRF Token** - สำหรับทุก form

---

## 📞 เมื่อไม่รู้จะทำยังไง

1. **ดู error message อย่างละเอียด**
   - ส่วนใหญ่ error message อธิบายปัญหาชัดเจน

2. **ใช้ test_connection.php**
   - ทดสอบ database connection ก่อน

3. **เปิด Browser Console (F12)**
   - เห็นเหตุที่แท้จริงของปัญหา

4. **ตรวจสอบไฟล์ logs**
   - Apache error log: `htdocs/error.log`
   - MySQL error log: ใน phpMyAdmin

5. **ลองทดสอบขั้นตัดแตกสิ่ง**
   - Test database connection
   - Test form submission
   - Test database data

---

**Last Updated:** 2025
**Version:** 1.0
