# 📋 HR System - คู่มือติดตั้งและใช้งาน

## ✅ สิ่งที่ต้องเตรียม

### ก่อนเริ่มต้น:
1. **Web Server**: Apache หรือ nginx ที่รองรับ PHP
2. **PHP**: เวอร์ชัน 7.0 ขึ้นไป
3. **MySQL**: เวอร์ชัน 5.7 ขึ้นไป
4. **phpMyAdmin** (เลือก): เพื่อความสะดวก

---

## 🚀 ขั้นตอนการติดตั้ง

### **Step 1: ทดสอบการเชื่อมต่อฐานข้อมูล**

1. เปิด Browser ไปที่:
   ```
   http://localhost/path-to-your-project/test_connection.php
   ```

2. ตรวจสอบผลลัพธ์:
   - ✅ ถ้า `"connection": "SUCCESS"` - ทำได้! ข้ามไป Step 2
   - ❌ ถ้ามี error - ดูหัวข้อ "แก้ไขปัญหา" ด้านล่าง

---

### **Step 2: ลองลงทะเบียนครั้งแรก**

1. เปิด Browser ไปที่:
   ```
   http://localhost/path-to-your-project/register.html
   ```

2. กรอกข้อมูลตามที่ต้องการ:
   - ชื่อจริง: (ต้องระบุ)
   - นามสกุล: (ต้องระบุ)
   - อีเมล: (ต้องระบุ)
   - รหัสผ่าน: 8 ตัวอักษรขึ้นไป (ต้องระบุ)
   - แผนก: เลือกจากรายการ (ต้องระบุ)
   - ข้อมูลอื่นๆ: ไม่บังคับ

3. คลิก "บันทึกข้อมูล"

4. ถ้าสำเร็จ:
   - ✅ จะได้ข้อความ "ลงทะเบียนสำเร็จ!"
   - จะเปลี่ยนไปหน้า login.html โดยอัตโนมัติ

---

### **Step 3: เข้าสู่ระบบ**

1. ป้อนอีเมลและรหัสผ่านที่ลงทะเบียนไว้
2. คลิก "เข้าสู่ระบบ"
3. เข้าสู่หน้า Dashboard

---

### **Step 4: ใช้งาน Dashboard**

- **ดูรายชื่อพนักงาน**: แสดงอยู่ตรงกลางหน้า
- **แก้ไขข้อมูล**: คลิก "แก้ไข" ในแถวพนักงาน
- **ลบข้อมูล**: คลิก "ลบ" (มีการยืนยัน)
- **ออกจากระบบ**: คลิก "ออกจากระบบ"

---

## 🔧 แก้ไขปัญหา

### **ปัญหา 1: ไม่สามารถเชื่อมต่อฐานข้อมูล**

**สาเหตุ:**
- MySQL Server ไม่ทำงาน
- Credentials ไม่ถูกต้อง (username/password/hostname)

**แก้ไข:**

**สำหรับ XAMPP:**
```
1. เปิด XAMPP Control Panel
2. คลิก "Start" สำหรับ Apache และ MySQL
3. รอจนสถานะแสดง "Running" (สีเขียว)
```

**สำหรับ WAMP:**
```
1. คลิกไอคอน WAMP ที่ system tray
2. ตรวจสอบสถานะ: 
   - ควรแสดง "All Services Running"
   - ไอคอนควรเป็นสีเขียว (ไม่แดง/ส้ม)
```

**ตรวจสอบ Credentials ใน db_connect.php:**
```php
$servername = "localhost";  // หรือ 127.0.0.1
$username = "root";         // username MySQL
$password = "";             // password (ถ้าไม่มี ให้ว่างไว้)
$dbname = "hr_system";      // ชื่อฐานข้อมูล
```

---

### **ปัญหา 2: ข้อมูลอีเมลซ้ำ**

**สาเหตุ:** อีเมลนี้ลงทะเบียนไปแล้ว

**แก้ไข:** 
- ใช้อีเมลใหม่ต่างจากที่ลงทะเบียนไปแล้ว
- หรือลบข้อมูลเก่าก่อน (ผ่าน phpMyAdmin)

---

### **ปัญหา 3: ไม่สามารถดูรายชื่อพนักงาน (Dashboard ว่าง)**

**สาเหตุ:** 
- ยังไม่ลงทะเบียนพนักงาน
- Session หมดอายุ

**แก้ไข:**
- ลองลงทะเบียนพนักงานใหม่ 2-3 คน
- ถ้า session หมดแล้ว ให้เข้าสู่ระบบใหม่

---

### **ปัญหา 4: รหัสผ่านไม่ถูกต้อง**

**สาเหตุ:** Password ต้องมีความยาว 8 ตัวอักษรขึ้นไป

**แก้ไข:** ใช้รหัสผ่านที่ 8 ตัวอักษร เช่น:
- `password123`
- `abcd1234`
- `Test@1234`

---

## 📁 โครงสร้างไฟล์

```
project-folder/
├── index.html                 # หน้าแรก (ว่าง - สำหรับอนาคต)
├── register.html              # หน้าลงทะเบียน
├── login.html                 # หน้าเข้าสู่ระบบ
├── dashboard.html             # หน้าแสดงรายชื่อพนักงาน
├── edit_employee.html         # หน้าแก้ไขข้อมูล
│
├── db_connect.php             # การเชื่อมต่อฐานข้อมูล (AUTO-CREATE DB & TABLE)
├── test_connection.php        # ทดสอบการเชื่อมต่อ
│
├── save_employee.php          # API: ลงทะเบียนพนักงาน
├── login.php                  # API: เข้าสู่ระบบ
├── check_session.php          # API: ตรวจสอบ session
├── logout.php                 # API: ออกจากระบบ
├── get_employees.php          # API: ดึงรายชื่อพนักงาน
├── get_employee.php           # API: ดึงข้อมูลพนักงานคนเดียว
├── update_employee.php        # API: แก้ไขข้อมูล
├── delete_employee.php        # API: ลบข้อมูล
│
├── create_table.sql           # SQL สำหรับสร้างตาราด้วยมือ (ไม่จำเป็น)
└── db_config.php              # ไฟล์เก่า (ไม่ใช้แล้ว)
```

---

## 📊 โครงสร้างฐานข้อมูล

**Database Name:** `hr_system`

**Table Name:** `employees`

| Column | Type | Notes |
|--------|------|-------|
| `id` | INT | Primary Key (AUTO_INCREMENT) |
| `emp_id` | VARCHAR(50) | รหัสพนักงาน (UNIQUE) |
| `firstname` | VARCHAR(100) | ชื่อจริง |
| `lastname` | VARCHAR(100) | นามสกุล |
| `email` | VARCHAR(100) | อีเมล (UNIQUE) |
| `password` | VARCHAR(255) | รหัสผ่าน (เข้ารหัสแล้ว) |
| `phone` | VARCHAR(20) | เบอร์โทรศัพท์ |
| `birthdate` | DATE | วันเกิด |
| `department` | VARCHAR(50) | แผนก |
| `position` | VARCHAR(100) | ตำแหน่ง |
| `created_at` | TIMESTAMP | วันที่สร้าง |
| `updated_at` | TIMESTAMP | วันที่แก้ไข |

---

## 🔒 ความปลอดภัย

✅ **ใช้อยู่แล้ว:**
- Password Hashing: `password_hash()` + `password_verify()`
- SQL Injection Prevention: Prepared Statements
- Session Management: PHP SESSION
- Input Validation: Server-side validation

⚠️ **สำหรับการใช้งานจริง (Production):**
- ใช้ HTTPS (SSL/TLS)
- Set Cookie flags: `HttpOnly`, `Secure`, `SameSite`
- Add CSRF Token สำหรับฟอร์ม
- Rate Limiting สำหรับ Login
- Change default MySQL password

---

## 📞 ติดต่อสอบถาม

ถ้ามีปัญหา:
1. ดูหัวข้อ "แก้ไขปัญหา" ข้างบน
2. ทดสอบการเชื่อมต่อที่ `test_connection.php`
3. ตรวจสอบ Browser Console (F12 → Console) สำหรับ error message

---

**Updated:** 2025
**Version:** 1.0
